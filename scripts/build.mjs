import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { marked } from 'marked';
import { zip } from './zip.mjs';
const ROOT=process.cwd(), OUT=path.join(ROOT,'dist'), BASE='/schrodingers_civ/', ORIGIN='https://www.benamuwo.me';
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8');
const write=(p,s)=>{fs.mkdirSync(path.dirname(path.join(OUT,p)),{recursive:true});fs.writeFileSync(path.join(OUT,p),s);};
fs.rmSync(OUT,{recursive:true,force:true});fs.mkdirSync(OUT,{recursive:true});fs.cpSync('src/assets',path.join(OUT,'assets'),{recursive:true});
for(const file of ['styles.css','app.js','favicon.svg']) write('assets/'+file,read('src/'+file));
const chapters=JSON.parse(read('src/content/chapters.json'));
const story=read('src/content/tale-source.md');
const starts=[...story.matchAll(/^#{1,3} (?:PROLOGUE:|[IVX]+\. |EPILOGUE:).*$/gm)];
if(starts.length!==23) throw Error('Story must contain exactly 23 source sections');
chapters.forEach((c,i)=>{c.source=story.slice(starts[i].index,starts[i+1]?.index??story.length).trim();c.body=c.source.replace(/^.*\n/,'').replace(/\n---\s*$/,'').trim();});
const link=(p,label,cls='')=>`<a ${p.startsWith('assets/')&&/\.(pdf|md|zip|svg|csv)$/.test(p)?'download':''} ${cls?`class="${cls}"`:''} href="${BASE+p}">${label}</a>`;
const navItems=[['','Home'],['audit/','Audit'],['atlas/','Atlas'],['tale/','The Translator’s Tale']];
const slug=s=>s.replace(/<[^>]*>/g,'').toLowerCase().normalize('NFKD').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
function decorate(html){
 html=html.replace(/<h([23])>(.*?)<\/h\1>/gs,(_,n,t)=>`<h${n} id="${slug(t)}">${t}</h${n}>`);
 html=html.replace(/<table\b[^>]*>/g,'<div class="table-scroll" tabindex="0" role="region" aria-label="Scrollable source table"><table>').replace(/<\/table>/g,'</table></div>');
 html=html.replace(/<th>/g,'<th scope="col">');
 html=html.replace(/src="\.\.\/figures\//g,`src="${BASE}assets/figures/`);
 html=html.replace(/<p>(<img[^>]+>)<\/p>\s*<p>(Figure [\s\S]*?)<\/p>/g,'<figure>$1<figcaption>$2</figcaption></figure>');
 html=html.replace(/<p>(<img[^>]+>)<\/p>/g,'<figure>$1</figure>');
 html=html.replace(/<figure>(<img[^>]*src="([^"]+)"[^>]*>)/g,'<figure>$1<a class="figure-link" href="$2" target="_blank" rel="noopener">Open full-size figure ↗</a>');
 return html;
}
const md=s=>decorate(marked.parse(s));
function tocFrom(html){return [...html.matchAll(/<h([23]) id="([^"]+)">(.*?)<\/h\1>/gs)].map(m=>({href:'#'+m[2],label:m[3].replace(/<[^>]*>/g,''),sub:m[1]==='3'}));}
function downloads(name){return `<div class="actions">${link(`assets/papers/${name}.pdf`,'Download PDF ↓','button primary')}${link(`assets/papers/${name}.md`,'Markdown ↓','button')}${link('assets/publication-assets.zip','MD + figures ZIP ↓','text-link')}</div>`;}
const routes=[];
function page(route,{title,description,active='',body,toc=[],type='',eyebrow='',extraHead=''}){
 const navigation=navItems.map(([p,l])=>`<a href="${BASE+p}" ${active===p?'aria-current="page"':''}>${l}</a>`).join('');
 const contents=toc.map(t=>`<a ${t.sub?'class="sub"':''} href="${t.href}" ${t.current?'aria-current="page"':''}>${esc(t.label)}</a>`).join('');
 const localnav=`<p class="eyebrow">${eyebrow||'On this page'}</p><nav aria-label="Contents">${contents}</nav>`;
 const footer=`<footer class="site-footer"><span>Schrödinger’s Civilization<br><small>Benjamin Amuwo · 2026</small></span><a href="https://github.com/RavenSeldon/shrodingers_civ.git">Research repository ↗</a>${link('about/','About the editions')}<span class="motto">Per Aspera, Ad Astra.</span></footer>`;
 const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="color-scheme" content="dark"><title>${esc(title)} — Schrödinger’s Civilization</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${ORIGIN+BASE+route}"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${ORIGIN+BASE+route}"><meta property="og:type" content="website"><link rel="icon" href="${BASE}assets/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="${BASE}assets/styles.css"><script src="${BASE}assets/app.js" defer></script>${extraHead}</head><body class="${type} ${toc.length?'with-rail':''}"><a class="skip" href="#main">Skip to content</a><div id="page-shell"><header class="topbar">${link('','<span class="brand-symbol" aria-hidden="true">✳</span><span>Schrödinger’s<br><strong>Civilization</strong></span>','brand')}<nav class="topnav" aria-label="Main navigation">${navigation}</nav><button class="menu-button" aria-haspopup="dialog" aria-controls="navigation-dialog" aria-expanded="false">Menu <span aria-hidden="true">☰</span></button></header>${toc.length?`<aside class="rail">${localnav}<div class="rail-bottom">A record. An audit. A tale.<br><span>Per Aspera, Ad Astra.</span></div></aside>`:''}<main id="main" tabindex="-1">${body}</main>${footer}</div><dialog id="navigation-dialog" aria-labelledby="menu-title"><div class="drawer-head"><h2 id="menu-title">Explore</h2><button class="close-menu" aria-label="Close menu">Close ×</button></div><nav class="drawer-global" aria-label="Main navigation">${navigation}</nav>${toc.length?localnav:''}</dialog></body></html>`;
 write(route+'index.html',html);routes.push({route,title});
}
page('',{title:'Schrödinger’s Civilization',description:'A study of claim transmission, an audit of the language behind it, and the story of the people who carried the work.',body:`<section class="home-hero"><div class="hero-copy"><p class="eyebrow">Research · Language · Human & machine</p><h1>Schrödinger’s<br><em>Civilization</em><span class="title-dot">.</span></h1><p class="home-intro">What survives the telling?</p><p class="lede">An incident becomes a public record. A record becomes a story. Along the way, distinctions can disappear—and a research project can develop a language of its own.</p><a class="text-link" href="#publications">Explore the work <span aria-hidden="true">↘</span></a></div><div class="hero-art"><img class="hero-seal" src="${BASE}assets/identity/maitrism-03-seal-final.svg" width="512" height="512" alt="Maitrism Seal: an ornate eight-node wheel surrounding the central Maitrism glyph" fetchpriority="high"><div class="art-label"><span>THE RECORD / THE REGISTER / THE TALE</span><span>01—03</span></div></div><div class="hero-meta"><span>Benjamin Amuwo</span><span>AI Incident Response Sprint · September 2026</span><span>Per Aspera, Ad Astra.</span></div></section><section class="publication-list" id="publications"><div class="section-heading"><p class="eyebrow">Three ways into one project</p><h2>The work, and the world<br>that grew around it.</h2></div><article class="publication"><span class="publication-number">01 / AUDIT</span><div class="publication-copy"><p class="eyebrow">Minded-Language Audit · v1.0</p><h3>${link('audit/','Who is speaking when<br>a Worker speaks?')}</h3><p>The project turns its deferred minded-language instrument on its own production record. Four tiers of vocabulary, a publication boundary, and the limits of what a lexical audit can tell us.</p><div class="actions">${link('audit/','Enter the dossier ↗','text-link')}${link('audit/paper/','Read the paper','text-link')}</div></div><a class="publication-image" href="${BASE}audit/" aria-label="Explore the Minded-Language Audit"><img src="${BASE}assets/figures/audit-1.svg" alt="Severity profiles across six language registers" loading="lazy"></a></article><article class="publication"><span class="publication-number">02 / ATLAS</span><div class="publication-copy"><p class="eyebrow">Claim Transmission Atlas · v1.0</p><h3>${link('atlas/','How Faithfully Did the Press Transmit the Record?')}</h3><p>A preregistered audit of 24 articles and four claim families. Two clocks distinguish what was faithful when published from what became stale as the record changed.</p><div class="actions">${link('atlas/','Explore the Atlas ↗','text-link')}${link('atlas/paper/','Read the paper','text-link')}</div></div><a class="publication-image atlas-thumb" href="${BASE}atlas/" aria-label="Explore the Claim Transmission Atlas"><img src="${BASE}assets/figures/atlas.svg" alt="The full time, taxonomy and state map" loading="lazy"></a></article><article class="publication tale-feature"><span class="publication-number">03 / TALE</span><div class="publication-copy"><p class="eyebrow">The Translator’s Tale · 23 chapters</p><h3>${link('tale/','With Effort,<br>to the Stars')}</h3><p>A history of the Claim Transmission Atlas, and of the people who carried it. The prose, the poetry, and the truth beneath the mythology.</p><div class="actions">${link('tale/prologue/','Begin the tale ↗','text-link')}${link('tale/','Chapter index','text-link')}</div></div><a class="publication-image" href="${BASE}tale/prologue/" aria-label="Begin the Translator’s Tale"><img src="${BASE}assets/art/i.webp" alt="A solitary figure looks into a star-filled sky" loading="lazy"></a></article></section><section class="closing-note"><p class="eyebrow">Reality before myth</p><p>The Workers are AI work roles.<br>The human is the human operator.<br><em>The poetry must be preserved,<br>but the facts more fiercely.</em></p><span>— The Translator’s Tale, Prologue</span></section>`});
let dossier=read('src/content/dossier.html').replaceAll('@@BASE@@',BASE);
// Source content retains its separate severity colour vocabulary.
dossier=dossier.replace('</header>',`<div class="edition-meta">Minded-Language Audit v1.0 · Guided dossier</div>${downloads('audit')}${link('audit/paper/','Read the complete paper →','text-link')}</header>`);
dossier=decorate(dossier).replace(/(<img src="([^"]+audit-[123]\.svg)"[^>]*>)/g,'$1<a class="figure-link" href="$2" target="_blank" rel="noopener">Open full-size figure ↗</a>');
const dossierToc=[{href:'#ladder',label:'The severity ladder'},{href:'#finding',label:'One project, four registers'},...([...dossier.matchAll(/<section id="([^"]+)">\s*<h2(?:[^>]*)>(.*?)<\/h2>/gs)].map(m=>({href:'#'+m[1],label:m[2].replace(/&rsquo;/g,'’')})))];
page('audit/',{title:'Who is speaking when a Worker speaks?',description:'The Minded-Language Audit dossier: method, findings, confidence and limitations.',active:'audit/',type:'research audit',eyebrow:'The dossier',toc:dossierToc,body:`<article class="dossier">${dossier}<section class="reader-download"><h2>Read the complete paper</h2><p>The dossier is a guided entry. The full nine-page reading edition includes the paper’s complete methods, references and appendices.</p>${downloads('audit')}${link('audit/paper/','Open the full-paper reader →','text-link')}</section></article>`});
for(const name of ['audit','atlas']){
 const source=read(`src/content/${name}.md`);write(`assets/papers/${name}.md`,source);
 const html=md(source.replace(/^.*\n\n/,'').replace(/^# .*\n/m,''));
 const title=name==='audit'?'Who is speaking when a Worker speaks?':'How Faithfully Did the Press Transmit the Record?';
 page(`${name}/paper/`,{title,description:`Complete ${name==='audit'?'Minded-Language Audit':'Claim Transmission Atlas'} paper, with PDF and Markdown reading editions.`,active:name+'/',type:'research paper '+name,toc:tocFrom(html),eyebrow:'In the paper',body:`<header class="reader-header"><p class="eyebrow">${name==='audit'?'Minded-Language Audit':'Claim Transmission Atlas'} · v1.0 · Full paper</p><h1>${title}</h1><p class="edition-meta">${name==='audit'?'Benjamin Amuwo':'Ben Amuwo'} · With Apart Research · ${name==='audit'?'9':'6'} source pages</p>${downloads(name)}<p class="reading-note">PDF and Markdown include project links. ${link('about/','Edition and source notes →')}</p></header><article class="prose paper-prose">${html}</article><div class="reader-download">${downloads(name)}</div>`});
}
function csvRows(s){const rows=[];let row=[],v='',q=false;for(let i=0;i<s.length;i++){const c=s[i];if(c==='"'){if(q&&s[i+1]==='"'){v+='"';i++;}else q=!q;}else if(c===','&&!q){row.push(v);v='';}else if(c==='\n'&&!q){row.push(v.replace(/\r$/,''));rows.push(row);row=[];v='';}else v+=c;}if(v){row.push(v);rows.push(row);}return rows;}
function table(headers,rows,caption){return `<div class="table-scroll" tabindex="0" role="region" aria-label="${esc(caption)}"><table><caption>${caption}</caption><thead><tr>${headers.map(x=>`<th scope="col">${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>'<tr>'+row.map((x,i)=>i?`<td>${esc(x)}</td>`:`<th scope="row">${esc(x)}</th>`).join('')+'</tr>').join('')}</tbody></table></div>`;}
const family=csvRows(read('src/assets/tables/table_02_claim_family_results.csv'));
let sourceTables=fs.readdirSync('src/assets/tables').sort().map((f,i)=>{const rows=csvRows(read('src/assets/tables/'+f));return `<details><summary>${String(i+1).padStart(2,'0')} · ${f.replace(/^table_\d+_|\.csv$/g,'').replaceAll('_',' ')}</summary>${link('assets/tables/'+f,'Download source CSV ↓','text-link')}${table(rows[0],rows.slice(1),f.replace('.csv','').replaceAll('_',' '))}</details>`;}).join('');
page('atlas/',{title:'Claim Transmission Atlas',description:'Explore the frozen time, taxonomy and state map. Read the two-clock study and inspect its source tables.',active:'atlas/',type:'research atlas explorer',eyebrow:'Explore the Atlas',toc:[{href:'#atlas-view',label:'The full map'},{href:'#reading',label:'How to read it'},{href:'#findings',label:'The bounded result'},{href:'#source-tables',label:'Source tables'},{href:'#paper',label:'Read the paper'}],body:`<header class="reader-header"><p class="eyebrow">Claim Transmission Atlas · v1.0</p><h1>One record.<br>Two clocks.</h1><p class="lede">How faithfully did the press transmit the record?<br>Follow the distinctions through time.</p><p class="edition-meta">Ben Amuwo · With Apart Research · September 2026</p><div class="actions">${link('atlas/paper/','Read the paper ↗','button primary')}${link('assets/figures/atlas.svg','Original SVG ↓','button')}</div></header><section id="atlas-view" class="atlas-section"><div class="section-bar"><h2>The Claim Transmission Atlas</h2><span>Time / taxonomy / state</span></div><p class="viewer-boundary">Lines connect claims, articles and evidence states. They do not imply article-to-article transmission or causal ancestry.</p><div class="atlas-viewer" data-viewer><div class="viewer-toolbar"><button data-zoom="in" aria-label="Zoom in">＋</button><button data-zoom="out" aria-label="Zoom out">−</button><button data-fit>Fit / reset</button><button data-pan aria-pressed="false">Pan mode</button><span data-scale role="status" aria-live="polite">100%</span><a href="${BASE}assets/figures/atlas.svg" target="_blank" rel="noopener">Full-size ↗</a></div><div class="viewer-window" tabindex="0" role="region" aria-label="Atlas figure viewer" aria-describedby="viewer-help"><img src="${BASE}assets/figures/atlas.svg" width="5000" height="3620" alt="Claim Transmission Atlas: four claim trunks, 87 article-claim trees, represented surface and body units, and Clock A and B states. Use the source tables below for a text alternative." draggable="false"></div><div class="viewer-pan-controls"><button data-move="left" aria-label="Pan left">←</button><button data-move="up" aria-label="Pan up">↑</button><button data-move="down" aria-label="Pan down">↓</button><button data-move="right" aria-label="Pan right">→</button></div></div><p class="reading-note" id="viewer-help">Zoom with + / −, then use the arrow controls or drag in Pan mode. With the viewer focused, use + / −, arrow keys and Home to reset. Pan mode enables touch dragging and pinch zoom; turn it off to scroll the page normally.</p><noscript><p>The full vector remains visible. <a href="${BASE}assets/figures/atlas.svg">Open the original SVG</a> to enlarge it.</p></noscript></section><section class="prose" id="reading"><p class="eyebrow">The visual grammar</p><h2>How to read the map</h2><p>A claim trunk leads to an article twig, then a SURFACE/BODY fork, a Clock-A node and a Clock-B node. Publication dates anchor articles in time. Local spacing within each article’s branch serves legibility; it does not encode another time interval.</p><div class="clock-pair"><div><span class="eyebrow">Clock A</span><h3>What was public then?</h3><p>Fidelity to the record available when the article appeared, under the frozen grace rule.</p></div><div><span class="eyebrow">Clock B</span><h3>What was known later?</h3><p>Fidelity to the later frozen record. Fair early reporting can become stale as evidence changes.</p></div></div><p>The trunk’s warm-to-cool colour tracks the visible public-record window. <strong>July 16 marks victim disclosure / the visible-time origin, not the incident date.</strong> Article colour identifies the frozen corpus stratum; shapes and fill carry evidence state.</p><ul class="stratum-key"><li><i class="general"></i> General</li><li><i class="business"></i> Business / mainstream technology</li><li><i class="specialist"></i> Specialist technology / cybersecurity</li></ul><p class="reading-note">Expanded 87-tree legibility export from the frozen submission assets. The complete legend is inside the figure. ${link('about/#atlas-selection','Selection and provenance →')}</p></section><section class="prose" id="findings"><p class="eyebrow">The bounded result</p><h2>Most distinctions survived.</h2><p>Established contemporaneous material drift occurred in <strong>7 of 83 assessable BODY observations (8.4%)</strong>. Both BODY BORDERLINE cases were adjudicated to non-drift; treating both as drift gives an upper sensitivity bound of 10.8%.</p>${table(['Claim family','Assessable N','Faithful','Drift','Borderline','Adjudicated','Upper bound'],family.slice(1).map(r=>[r[1],r[2],r[3],r[4],r[5],r[13]+'%',r[11]+'%']),'Clock A · BODY observations by claim family')}<p>Four of seven established BODY drifts concerned motive and objective. All 23 assessable BODY C5 observations were faithful at Clock A. Eight represented BODY observations were faithful at Clock A but drift at Clock B.</p><p class="qualification">Descriptive results from a bounded, outlet-weighted corpus of 24 English-language articles. Search completeness is unverified. The study does not support outlet rankings, journalist-intent claims or population-level inference.</p></section><section id="source-tables"><p class="eyebrow">Accessible record</p><h2>Inspect the source tables</h2><p>Seven frozen submission tables, reproduced directly. Column names and denominators are preserved; CSV downloads carry the original bytes.</p><div class="source-tables">${sourceTables}</div></section><section id="paper" class="reader-download"><p class="eyebrow">The complete study</p><h2>How Faithfully Did the Press Transmit the Record?</h2><p>A Preregistered Two-Clock Audit of Claim Drift in Coverage of the July 2026 OpenAI-Hugging Face Incident</p>${downloads('atlas')}${link('atlas/paper/','Open the full-paper reader →','text-link')}</section>`});
page('tale/',{title:'The Translator’s Tale',description:'With Effort, to the Stars. A history of the Claim Transmission Atlas in 23 illustrated chapters.',active:'tale/',type:'tale-index',body:`<header class="tale-index-hero"><img src="${BASE}assets/art/i.webp" alt="A human silhouette beneath the Milky Way" fetchpriority="high"><div><p class="eyebrow">The Translator’s Tale</p><h1>With Effort,<br><em>to the Stars.</em></h1><p>A History of the Claim Transmission Atlas,<br>and of the People Who Carried It</p><p class="motto">Per Aspera, Ad Astra.</p>${link('tale/prologue/','Begin with the Prologue →','button primary')}</div></header><section class="chapter-index"><div class="section-heading"><p class="eyebrow">23 chapters · One complete tale</p><h2>Before the constellations,<br>there was the work.</h2><p>These names became meaningful because they gave form to something real.</p></div><ol>${chapters.map(c=>`<li><a href="${BASE}tale/${c.slug}/"><span class="chapter-n">${c.stem}</span><img src="${BASE}assets/art/${c.file}" width="${c.width}" height="${c.height}" alt="" loading="lazy"><span>${c.title.toLowerCase().replace(/(^|\s)\S/g,t=>t.toUpperCase())}</span><b aria-hidden="true">↗</b></a></li>`).join('')}</ol>${link('assets/papers/tale.md','Download the complete story · Markdown ↓','text-link')}</section>`});
chapters.forEach((c,i)=>{
 const prev=chapters[i-1],next=chapters[i+1];
 const intro=i===0?`<div class="story-source-title">${marked.parse(story.slice(0,starts[0].index).replace(/^# /m,'## ').replace(/^## A History/m,'### A History'))}</div>`:'';
 const body=`<div class="tale-backdrop" aria-hidden="true" style="--focal:${c.focal}"><img src="${BASE}assets/art/${c.file}" width="${c.width}" height="${c.height}" alt="" fetchpriority="high"></div><div class="chapter-top"><a href="${BASE}tale/">← All chapters</a><span>${String(i+1).padStart(2,'0')} / 23</span><button class="art-open" data-art="${BASE}assets/art/${c.file}">View artwork ↗</button></div><article class="chapter"><header><p class="eyebrow">The Translator’s Tale <span>/</span> ${c.stem}</p><h1>${c.title.toLowerCase().replace(/(^|\s)\S/g,t=>t.toUpperCase())}</h1><div class="chapter-rule"></div></header>${intro}<div class="story-prose">${marked.parse(c.body)}</div><nav class="chapter-pagination" aria-label="Chapter navigation">${prev?`<a rel="prev" href="${BASE}tale/${prev.slug}/"><span>← Previous · ${prev.stem}</span>${prev.title.toLowerCase()}</a>`:`<a href="${BASE}tale/">← Chapter index</a>`}${next?`<a rel="next" href="${BASE}tale/${next.slug}/"><span>Next · ${next.stem} →</span>${next.title.toLowerCase()}</a>`:`<a href="${BASE}tale/"><span>The end</span>Return to the constellations →</a>`}</nav></article><div class="reading-progress" aria-hidden="true"><span></span></div><dialog class="art-dialog" aria-label="Chapter artwork"><button class="art-close" aria-label="Close artwork">Close ×</button><img src="${BASE}assets/art/${c.file}" alt="Artwork for ${esc(c.title)}"><p>${esc(c.title)} · Supplied chapter artwork</p></dialog>`;
 page(`tale/${c.slug}/`,{title:c.title.toLowerCase().replace(/(^|\s)\S/g,t=>t.toUpperCase()),description:`Chapter ${c.stem} of The Translator’s Tale: With Effort, to the Stars.`,active:'tale/',type:'tale chapter-page',eyebrow:'The chapters',toc:chapters.map(x=>({href:BASE+'tale/'+x.slug+'/',label:x.stem+' · '+x.title.toLowerCase(),current:x===c})),body});
});
page('about/',{title:'About the editions',description:'Publication sources, reading editions and the limits of this presentation.',type:'research',toc:[{href:'#editions',label:'Reading editions'},{href:'#atlas-selection',label:'Atlas selection'},{href:'#illustrations',label:'Illustrations'},{href:'#source-boundary',label:'Source boundaries'}],body:`<header class="reader-header"><p class="eyebrow">A note on the record</p><h1>Sources remain<br>sources.</h1><p class="lede">This website presents two publications and a narrative. It does not recompute their research or revise their findings.</p></header><article class="prose"><section id="editions"><h2>Reading editions</h2><p>The Minded-Language Audit uses the author-supplied nine-page reading edition: dark-on-dark text was repaired and the final review-items page was removed before this website was built. The retained research content and figures are preserved.</p><p>The Atlas reader is derived from the supplied six-page final PDF. The repository’s differently worded report Markdown was not used. The final PDF retains “submission draft” running footers, placeholder language in Code and Data, and an Author Review Items section. Those source statements remain in the reading edition; the canonical website and repository links are provided separately.</p><p>Both downloadable PDFs add clickable project and repository URLs in the top margin on every page. Their body text, figures and pagination are retained. The Markdown editions reflow PDF text into paragraphs and tables and link to the figures. The companion ZIP keeps the Markdown and figure paths together for offline use.</p></section><section id="atlas-selection"><h2>Atlas selection</h2><p>The explorer uses the unmodified <code>submission/assets/claim_transmission_atlas.svg</code> at research revision <code>2e97880</code>. This is the expanded 5000 × 3620 legibility export with 87 article-claim trees. The older temperature-axis-named export contains 85 trees and was not selected.</p><p>The chronology correction and later legibility notes establish the interpretation: July 16 is victim disclosure and the visible-time origin; it is not the incident date. The later notes document semantic comparison against current coding, while explicitly qualifying the Artist’s access to the earlier parent bytes. This website’s inventory checks do not independently re-certify the study’s approval process.</p><p class="hash">SVG SHA-256<br>eac1e82817728b4df34d5455c20fdc2390a5d51b391693f10a9af72dc01a6f67</p></section><section id="illustrations"><h2>The illustrations</h2><p>All 23 images are paired by their supplied Roman-numeral or prologue/epilogue stem. Compressed WebP copies retain the original dimensions. The supplied files are 357–486 pixels wide; larger displays will reveal their softness. Full-resolution replacements can be added without changing the story or chapter links.</p><p>The complete source narrative is preserved, including its distinct voice, poetry and emphasis. The supplied preparatory text had already removed chat citation markers. The story’s original title is <em>With Effort, to the Stars</em>; “The Translator’s Tale” names this part of the website.</p></section><section id="source-boundary"><h2>Source boundaries</h2><p>The audit’s printed total of 178,909 words includes the human- and model-authored segmented subsets alongside the full internal record. The site preserves that source count without relabelling it as a deduplicated corpus size. The paper’s comparisons and confidence language remain the authors’ claims.</p><p>The source papers refer to larger research and audit packages. This publication site includes the selected papers, dossier, figures, seven frozen submission tables and story. It does not mirror worker archives or claim to contain the entire audit computation package.</p><p>${link('assets/source-manifest.json','Download the publication asset manifest →')}</p></section></article>`});
page('404/',{title:'Page not found',description:'This path does not lead to a publication or chapter.',body:`<section class="not-found"><p class="eyebrow">404 · An uncharted coordinate</p><h1>This page is<br>off the map.</h1><p>The record, the audit and the tale are still here.</p>${link('','Return to the project →','button primary')}</section>`});
write('404.html',read('dist/404/index.html'));
write('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${routes.filter(r=>r.route!=='404/').map(r=>`<url><loc>${ORIGIN+BASE+r.route}</loc></url>`).join('')}</urlset>`);
write('routes.json',JSON.stringify(routes,null,2));
function files(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?files(path.join(dir,e.name)):[path.join(dir,e.name)]);}
zip(files(path.join(OUT,'assets')).filter(p=>p.endsWith('.md')||p.includes('/figures/')).map(p=>({name:path.relative(path.join(OUT,'assets'),p),data:fs.readFileSync(p)})),path.join(OUT,'assets/publication-assets.zip'));
const manifest=files(path.join(OUT,'assets')).map(p=>({path:path.relative(OUT,p),sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex'),bytes:fs.statSync(p).size}));
write('assets/source-manifest.json',JSON.stringify({researchRevision:'2e978808cc7f007a7dd07bf14d91249469ebe9c4',canonical:ORIGIN+BASE,assets:manifest},null,2));
console.log(`Built ${routes.length} static routes at ${BASE}; ${manifest.length} publication and interface assets.`);
